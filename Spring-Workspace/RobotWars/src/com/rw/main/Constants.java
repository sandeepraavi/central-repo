package com.rw.main;

public final class Constants {
	/*
	 * Declaring the max values for the position. Since the grid is 5X5 the min
	 * and max coordinate values for x and y can be only 0 and 4 respectively.
	 * These values can be changed depending on the grid value.
	 */
	public final int MIN_X = 0;
	public final int MIN_Y = 0;
	public final int MAX_X = 4;
	public final int MAX_Y = 4;
}
